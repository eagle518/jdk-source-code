shared.build.dir=${basedir}/build/shared
dist.dir=${shared.build.dir}/dist
build.dir=${shared.build.dir}/classes
ide.dir=${shared.build.dir}/ide-classes
src.dir=${basedir}/src/share/classes

ant.script=${basedir}/make/sun/javac/ant/build.xml
